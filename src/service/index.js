const authService = require('./auth.service');
const userService = require('./user.service');
const prodService = require('./prod.service');

module.exports = {
    authService,
    userService,
    prodService
}